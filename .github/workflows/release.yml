name: Release

# Build a release APK and publish it to GitHub Releases on a version tag.
# Modelled on reference/bitchat-android, adapted for Myco's Rust cross-compile
# (myco-core + the gitignored `fips` core) and optional release signing.
#
# Signing is OPT-IN via repo secrets (Settings → Secrets and variables → Actions):
#   RELEASE_KEYSTORE_BASE64  base64 of your myco-release.jks  (`base64 -i myco-release.jks | pbcopy`)
#   RELEASE_STORE_PASSWORD   keystore password
#   RELEASE_KEY_ALIAS        key alias (e.g. myco)
#   RELEASE_KEY_PASSWORD     key password
# Without them the build still runs and ships an *unsigned* APK.

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*.*.*'   # e.g. v0.1.0

env:
  FIPS_REPO: https://github.com/jmcorgan/fips.git
  FIPS_REF: fix/platform-ble   # fips branch carrying the mobile seams (Android BLE, per-instance transports)
  NDK_VERSION: 26.1.10909125

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write       # create the GitHub Release
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # `fips` is a gitignored local path dep (reference/fips). A fresh checkout
      # has no `reference/` at all, so clone the fork branch into place.
      - name: Fetch fips core
        run: git clone --depth 1 --branch "$FIPS_REF" "$FIPS_REPO" reference/fips

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android NDK ${{ env.NDK_VERSION }}
        run: sdkmanager "ndk;${NDK_VERSION}"

      - name: Set up Rust (aarch64-linux-android)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --locked

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-${{ runner.os }}-

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-${{ runner.os }}-

      # Decode the signing keystore if the secret is set; otherwise build unsigned.
      # Exporting MYCO_RELEASE_STORE_FILE flips on the Gradle release signingConfig.
      - name: Stage signing keystore
        env:
          KEYSTORE_B64: ${{ secrets.RELEASE_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_B64" ]; then
            echo "$KEYSTORE_B64" | base64 -d > "$RUNNER_TEMP/myco-release.jks"
            echo "MYCO_RELEASE_STORE_FILE=$RUNNER_TEMP/myco-release.jks" >> "$GITHUB_ENV"
            echo "Release will be SIGNED."
          else
            echo "No RELEASE_KEYSTORE_BASE64 secret — building an UNSIGNED release APK."
          fi

      # Derive the APK version from the tag: versionName = tag minus the leading
      # `v`; versionCode = the run number (monotonic, so updates are detectable).
      - name: Derive version from tag
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          echo "MYCO_VERSION_NAME=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
          echo "MYCO_VERSION_CODE=${GITHUB_RUN_NUMBER}" >> "$GITHUB_ENV"

      - name: Build release APK
        working-directory: android
        env:
          ANDROID_NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/${{ env.NDK_VERSION }}
          MYCO_RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          MYCO_RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          MYCO_RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: ./gradlew :app:assembleRelease --no-daemon --stacktrace

      - name: Stage APK for release
        run: |
          cd android/app/build/outputs/apk/release
          ls -lh ./*.apk
          apk=$(ls app-release*.apk | head -1)
          out="$GITHUB_WORKSPACE/myco-${GITHUB_REF_NAME:-dev}-arm64.apk"
          cp "$apk" "$out"
          echo "APK=$out" >> "$GITHUB_ENV"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: myco-release-${{ github.ref_name }}
          path: ${{ env.APK }}
          if-no-files-found: error

      # Use the hand-written RELEASE-NOTES.md as the release body, so the notes
      # we already maintain per release become the release page instead of a
      # bare commit list. Its leading `# Myco vX.Y.Z` / `**Released**:` header
      # duplicates the release title and date, so strip it. If the file is
      # missing or still describes an older tag, fall back to generated notes
      # rather than publishing something wrong.
      - name: Prepare release body
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          notes="$GITHUB_WORKSPACE/RELEASE-NOTES.md"
          if [ ! -f "$notes" ]; then
            echo "No RELEASE-NOTES.md — falling back to generated notes."
            exit 0
          fi
          if ! head -1 "$notes" | grep -qF "$GITHUB_REF_NAME"; then
            echo "RELEASE-NOTES.md does not mention $GITHUB_REF_NAME (first line: $(head -1 "$notes"))."
            echo "Falling back to generated notes rather than publishing stale notes."
            exit 0
          fi
          # Drop the `# Myco vX.Y.Z` heading, the `**Released**:` line, and the
          # blank lines around them; keep everything from the first section on.
          awk 'NR==1 && /^# /  {next}
               /^\*\*Released\*\*:/ {next}
               !printed && NF==0 {next}
               {printed=1; print}' "$notes" > "$RUNNER_TEMP/release-body.md"
          echo "RELEASE_BODY_PATH=$RUNNER_TEMP/release-body.md" >> "$GITHUB_ENV"

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.APK }}
          name: Myco ${{ github.ref_name }}
          # When body_path is set, generate_release_notes appends the
          # auto-generated "What's Changed" + full-changelog link beneath it.
          body_path: ${{ env.RELEASE_BODY_PATH }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- Optional: publish to Zapstore after the GitHub Release exists ---
      # Zapstore pulls the signed APK from the GitHub Release (release_source in
      # zapstore.yaml). Sign the Zapstore *event* with a remote signer (no nsec
      # in CI) by storing a NIP-46 bunker URL as the SIGN_WITH secret.
      #
      # - name: Publish to Zapstore
      #   if: startsWith(github.ref, 'refs/tags/')
      #   env:
      #     SIGN_WITH: ${{ secrets.ZAPSTORE_BUNKER_URL }}   # bunker://pubkey?relay=...&secret=...
      #   run: |
      #     go install github.com/zapstore/zsp@latest
      #     "$(go env GOPATH)/bin/zsp" publish

  desktop:
    name: Desktop bundles (.deb + AppImage)
    runs-on: ubuntu-latest
    permissions:
      contents: write       # attach artifacts to the GitHub Release
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch fips core
        run: git clone --depth 1 --branch "$FIPS_REF" "$FIPS_REPO" reference/fips

      - name: Install build deps
        # bindgen (libclang) + the Tauri (wry/webkit2gtk) system libraries the
        # bundle links and the AppImage packages.
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libdbus-1-dev libclang-dev \
            libwebkit2gtk-4.1-dev libgtk-3-dev librsvg2-dev \
            libayatana-appindicator3-dev libsoup-3.0-dev

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo + target
        uses: Swatinem/rust-cache@v2

      - name: Install tauri-cli
        run: cargo install tauri-cli --version "^2.0" --locked

      - name: Build bundles
        working-directory: desktop/src-tauri
        run: cargo tauri build --bundles deb,appimage

      - name: Collect bundles
        run: |
          mkdir -p dist
          cp target/release/bundle/deb/*.deb dist/
          cp target/release/bundle/appimage/*.AppImage dist/
          ls -lh dist/

      - name: Upload bundle artifacts
        uses: actions/upload-artifact@v4
        with:
          name: myco-desktop-${{ github.ref_name }}
          path: dist/*
          if-no-files-found: error

      # Attach the desktop bundles to the same tagged GitHub Release the APK job
      # creates. action-gh-release is idempotent per tag: files are added to the
      # existing release, and omitting name/body leaves the APK job's metadata
      # untouched.
      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
